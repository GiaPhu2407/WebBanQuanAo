import { Home, Search, ShoppingCart, User, AlertCircle } from "lucide-react";

export const Icons = {
  home: Home,
  search: Search,
  cart: ShoppingCart,
  user: User,
  alertCircle: AlertCircle,
  logo: AlertCircle, // Replace with your desired logo icon
};
