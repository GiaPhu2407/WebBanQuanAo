/** @type {import('next').NextConfig} */
const nextConfig = {};
// next.config.js
module.exports = {
  output: "export", // hoặc cấu hình khác nếu cần
  // các tùy chọn khác nếu có
};

export default nextConfig;
